/target
lcov.info
coverage.out

# The Chatterbox TTS server (travisvn/chatterbox-tts-api) is a separate
# Python project, not part of this repo - it ended up cloned here by
# accident during setup and is kept ignored until it's moved out.
/chatterbox-tts-api

# Piper binary and voice models: downloaded by scripts/piper-setup.ps1,
# not checked in (a binary plus ~60 MB of model weights don't belong in
# the repo).
/tools/piper/
/data/voices/piper/*.onnx
/data/voices/piper/*.onnx.json

# Whisper GGML models: downloaded by scripts/stt-setup.ps1, not checked in
# (100 MB-3 GB depending on size, don't belong in the repo).
/data/whisper/
